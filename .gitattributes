# Force LF line endings. This repo is edited on Windows but its contents are
# consumed by Linux tooling (WSL, Docker builds, Kubernetes). A CRLF shebang
# fails with a misleading "bad interpreter: No such file or directory".
* text=auto eol=lf
